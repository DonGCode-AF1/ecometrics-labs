# 2026-05-29 DML Cross-Fitting Lab

## Task

Move the local `preview.html` teaching page into the econometrics lab repository, clarify its learning purpose, and publish it as a standalone web lab.

## Implementation Notes

- Placed the page under `labs/dml-cross-fitting/`.
- Renamed the dashboard to `dml_cross_fitting_dashboard.html`.
- Clarified the page title and lead around out-of-fold residuals and causal estimation.
- Added a short reading-order section connecting cross-fitting to the Neyman orthogonality DML lab.
- Added the lab to the repository landing page and README catalog.

## Validation

The page should run as a standalone HTML dashboard with no external build step. It contains native canvas visualizations, generated sample/residual tables, and minimal Python template code.
